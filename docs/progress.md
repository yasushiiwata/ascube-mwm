# 進捗

| 日付 | タスク | 受入条件 | 残課題 |
|---|---|---|---|
| 2026-09-04 | 環境構築 | Git 2.55 / .NET 10 SDK / Claude Code 2.1.258 / DCMTK 3.7.0 | — |
| 2026-09-05 | T0-S1（.NET10+fo-dicom最小SCP） | ○ 合格。echoscu / findscu（DCMTK・独立実装）とも Success。TFM は net10.0 のまま継続。 | なし |
| 2026-09-05 | T0-S2（文字コード往復検証） | △ 部分合格。`ISO_IR 192`（UTF-8）は漢字・外字(髙﨑德)・濁点・長音・30文字級すべて dcmdump（独立実装）で完全一致。`ISO 2022 IR 13\ISO 2022 IR 87` は **× 不合格**：fo-dicomの既定PN書き込みはコード拡張エスケープ(1B 24 42 / 1B 29 49)を一切出力せず、実体は生の CP932(Shift_JIS) バイト列を charset ラベルだけ ISO2022 と偽って書き出していた。DCMTK dcmdump は "Illegal byte sequence" で読み込み自体を拒否。 | **T7で自前 byte[] PN エンコーダに切替が確定。2〜3日を確保すること。** 詳細は `spikes/S2_charset_roundtrip/out/report*.md`。 |
| 2026-09-05 | T0-S3（生バイト記録） | ○ 合格（②を採用）。①`DicomServiceOptions.LogDataPDUs/LogDimseDatasets`は構造化ログのみで生バイト復元不可のため不採用。②DI経由で`INetworkManager`を差し替え（`AddFellowOakDicom()`の**後**に`AddNetworkManager<T>()`）、`INetworkStream.AsStream()`をteeする`Stream`デコレータで送受信バイトをbounded channel経由で完全capture。fo-dicom本体は無改変。③(透過TCPプロキシ)は不要と判断。 | T9実装時に本スパイクの`RawRecorder`/`RecordingNetworkManager`パターンを流用する。詳細は`spikes/S3_raw_recorder/out/S3_conclusion.md`。 |
| 2026-09-04 | T0 | S-1 ○・S-2 一部×（fo-dicom が ISO 2022 のエスケープを出力しない）・S-3 ○ | 残課題：案④(ISO_IR 13 単独値)の検証 |
| 2026-09-06 | T0/alt4 | ○ 合格。`ISO_IR 13` 単独値で半角カナ（濁点ﾀﾞ・半濁点ﾊﾟ・長音ｰ・中点･・30文字級）が JIS X 0201 の 0xA1〜0xDF の1バイトで正しく出力され、エスケープシーケンスも出ず、DCMTK dcmdump が警告なく読める。漢字混入ケースは fo-dicom の自己再読込だけでは不一致を検出できない（内部でCP932相当に静かにフォールバックする）が、DCMTK dcmdump は読み込み自体を拒否し不一致を検出できた。 | T7では自己再読込に頼らず encode→decode の往復検証を自前実装し `Suppressed` 判定に使うこと。実機(APEX 5.6)では未検証。詳細は `spikes/S2_charset_roundtrip/alt4_result.md`。 |
| 2026-09-09 | T1（ソリューション骨格・設定読込・バリデータ） | ○ 合格。ブランチ `task/T1-skeleton`。Abstractions/Store/Core/Scp/Tools ＋ tests 3本（Core.Tests に検証ロジックを実装、Store.Tests/Scp.Tests は T2/T3 向けの空スケルトン）を作成。JSONC ローダ（コメント・末尾カンマ許容）＋ `_base.jsonc` との深いマージ（オブジェクトは再帰、配列は置換）を実装。バリデータは指示書どおりの順序（schemaVersion→タグ形式→DICOM辞書VR一致→VM→SQ入れ子2階層以内→db:列存在→charset既知語→ISO2022禁止・禁止式検出）で実装し、`dotnet build`/`dotnet test` とも成功（38/38）。`config/profiles/` は _base/GENERIC/BMD_HOLOGIC/.alt1/.alt2/ES_DEFAULT の6枚（ISO 2022 系プロファイルは作らず）。わざと壊した6パターン（括弧欠落・未知タグ・VR誤り・存在しない列・不正charset・ISO2022を含むcharset）は `tests/Ascube.Mwm.Core.Tests/Fixtures/broken/` に用意し、全パターンで具体的な JSON パスつきの検証失敗を確認。`mwm-admin admin validate --profile <id>` を実装し、正常系・異常系とも手動実行で確認済み。 | `docs/開発計画_v2.md`（プロファイルの根拠文書）がリポジトリに存在しないため、`dataset.elements` の DSL（`db:`/`const:`/`echo:`/`uid:study`/`coalesce:`/`auto:*`、SQ構造）は実装指示書 v2・設計変更メモ_v2.1・依頼電文マッピング表から再構築した独自設計。T2以降で実データ・実運用と突き合わせて必要なら見直すこと。`IWorklistRepository`/`WorkItemView`/`QueryCriteria`/`ExplainResult`（T4/T5/T8で使用）はまだ作っていない（仕様未確定のため半端な実装を避けた）。 |
| 2026-09-09 | T2（SQLiteストア＋UID採番） | ○ 合格。ブランチ `task/T2-sqlite-store`（`task/T1-skeleton` の続き。**T1はまだmainに未マージ**なので同ブランチから分岐）。DDLは`CurrentEntry`＋`Patient`/`WorkItem`/`UidAllocation`の4テーブル（`AuditCFind`/`AuditCFindItem`はT8で追加）。`IWorklistWriter.SetCurrentAsync`は1トランザクションでPatient upsert→WorkItem upsert（`StablePatientId`+`ScheduledDate`+`DeviceProfileId`の複合UNIQUEキーで同一受診者を再利用）→UidAllocation採番（無ければ生成）→CurrentEntry差し替えを行う。UID生成は`2.25.`+GUID128bitの符号なし10進表記（`BigInteger`）。SCP側`IWorklistRepository`（`QueryAsync`/`ExplainAsync`）はTTL・存在確認のみを行う読み取り専用実装とし、`Mode=ReadOnly`＋`PRAGMA query_only=1`の両方で規則5を担保。TTL判定は`TimeProvider`を注入してテスト（時刻を注入して検証）。`dotnet build`/`dotnet test`とも成功（Store.Testsのみ17件、全体で55件）。受入条件（100回SetCurrentAsyncでWorkItem行/UID不変、A→B切替、Clear後0件、TTL失効後0件、10並行書込+10並行読取で例外なし=E-13・中途半端な行が見えない=E-14、UID正規表現一致・100万回重複ゼロ）を全てテストで確認。 | `IWorklistRepository.QueryAsync`/`ExplainAsync`は現時点では`QueryCriteria`（T5でフィールド追加予定、今はプロパティなし）を無視し「今の1人が生きているか」だけを見る素朴な実装。日付範囲・PatientID/PatientName指名・Modalityによる絞り込みはT5（MatchEngine）が担当する設計とした。`AuditCFind`/`AuditCFindItem`テーブルはT8まで未作成。 |
| 2026-09-10 | T3（C-ECHO SCP＋アソシエーション制御） | ○ 合格。ブランチ `task/T3-cecho-scp`（T1+T2 を先に `main` へ `--no-ff` マージしてから分岐）。`MwmDicomService : DicomService, IDicomServiceProvider, IDicomCEchoProvider` を実装。フローは指示書どおり「ルーティングでプロファイル解決→ログ出力→Calling AE照合(strict/logOnly/ignore)→PCごとにaccept/reject」。**ルーティングの仕様が `docs/開発計画_v2.md` 欠如のため repo 内に無かった**ので、Called AE Title でプロファイルを解決する設計を新規に定義（`DeviceProfile.AeTitle`/`ScpRoutingContext.ResolveByCalledAe`）。同様に「Calling AE の許可リスト」も schema に無かったため `network.allowedCallingAeTitles`（string[]）を追加し、`callingAeMatching=strict/logOnly` では非空を必須化するバリデーションを足した（規則8：現場の1文字ミスで検査停止させないため、空リストのまま有効化させない）。Transfer Syntax は `pc.AcceptTransferSyntaxes(...)` に委譲（未サポート分だけ自動 reject、規則9）。Abstract Syntax は Verification 以外を `RejectAbstractSyntaxNotSupported`（C-FIND は T4）。規則10（黙って切らずAssociate-RJ）は `DicomRejectReason.CalledAENotRecognized`/`CallingAENotRecognized` で実装し、DCMTK `echoscu`（独立実装）で "Called AE Title Not Recognized" を実際に確認。規則3（応答送信中の一方的切断は正常系）は `OnConnectionClosed` で無条件に握りつぶし、FIN/RST/データ無し切断の3パターンでリスナが生き続けることをテストで確認（`AbruptDisconnectTests`）。Windows サービス化は `Microsoft.Extensions.Hosting.WindowsServices` の `AddWindowsService`（`--console` で無効化）。`dotnet build`/`dotnet test` とも成功（Scp.Tests 13件追加、全体80件）。DCMTK `echoscu`/`findscu` で手動疎通確認済み（正常Echo・Called AE不一致reject・未実装のC-FINDが「他が生きていれば道連れにならない」ことを確認）。 | Serilog は今回配線しなかった（`Directory.Packages.props` には `Serilog`/`Serilog.Sinks.File` があるが `Serilog.Extensions.Hosting` 系のブリッジパッケージが無く、今回のスコープでは `Microsoft.Extensions.Logging` 既定のコンソールログで規則の要求は満たせたため）。ファイルへの構造化ログ出力が要る運用タスク（T12近辺）で改めて検討する。`ScpHostedService` はプロファイルをポート単位でグルーピングして複数ポート対応できる作りにしてあるが、現状 `--profile BMD_HOLOGIC` の1枚構成でしか実運用していない。 |
| 2026-09-10 | T4（C-FIND最小・固定1件） | ○ 合格。ブランチ `task/T4-cfind-minimal`（T3を`main`へマージ後に分岐）。`MwmDicomService` に `IDicomCFindProvider` を追加し、Modality Worklist FIND の Abstract Syntax（`1.2.840.10008.5.1.4.31`）も PC 受理対象に加えた（T3では Verification のみだった）。`OnCFindRequestAsync` は指示書どおりハードコードした1件を `Pending` で返してから `Success` を返すだけ（実データ照合はT5、実データ組み立てはT6で置き換える。コードにもその旨コメント済み）。ハードコード値は SpecificCharacterSet を先頭に置く（規則6）ダミーの氏名・患者ID・SQ（ScheduledProcedureStepSequence）を含む。`dotnet test` で新規2件含め全体82件成功。DCMTK `findscu -W -k "0008,0052=WORKLIST"` で1件のPending応答→Successを確認し、応答データセットを `dcmdump`（独立実装）でSQ入れ子まで正しく読めることを確認（N-16）。 | **pcap採取は未実施。** 実装指示書は「ここで一度pcapを取り、以後の正解サンプルにする」と指示しているが、この開発環境に tshark/dumpcap/Wireshark が無く採取できなかった。DCMTKの `findscu -d`（デバッグ出力でPDUの生バイトを表示）とレスポンスの `dcmdump` 結果を代替のエビデンスとして本行に残す。Wireshark が使える環境（施設PC等）で、可能なら T9（raw stream recorder）完成後にアプリ内蔵の記録機能で採取するのでも良い。ハードコードしたPatientName/PatientID等は明らかなダミー値（規則4は実データ経路が無い段階のプレースホルダーには適用されないと解釈したが、T5/T6着手時に必ず置き換えること）。 |
| 2026-09-10 | T5（MatchEngine） | ○ 合格。ブランチ `task/T5-match-engine`（T4を`main`へマージ後に分岐）。`MatchEngine`（Range/Wildcard/Universal Matching・Modality判定）を `Ascube.Mwm.Abstractions` に純粋関数として実装（fo-dicom/SQLite非依存。BRIDGE-Navi本体にfo-dicomを持ち込まない制約を守るため）。`QueryCriteria` に `ScheduledDateRange`/`PatientId`/`PatientName`/`Modality` を追加し、`QueryCriteriaParser`（Core、fo-dicomのDicomDatasetからの取り出し）を新設。`SqliteWorklistRepository.QueryAsync`/`ExplainAsync`（Store）を更新し、TTL・存在確認だけだった素朴実装から実際にMatchEngineで絞り込むように変更（ExplainAsyncは日付範囲・PatientID不一致時に要求値と保持値を含む理由文字列を返す＝T8 explain-queryの土台）。Modality照合は候補側の値がDB由来ではなくプロファイル設定（dataset.elementsの(0008,0060)const:値）なので、`DeviceProfile.ScheduledStationModality`/`ModalityMatching`を新設し、Scp層（`MwmDicomService.OnCFindRequestAsync`）で`MatchEngine.EvaluateModality`を別途呼ぶ構成にした。`OnCFindRequestAsync`もT4のハードコード返却をやめ、実際にRepositoryから取得した候補をMatchEngineで判定→一致すれば実データからデータセットを組み立てて返すように書き換えた（PatientNameが欠損する行は規則4により返さずSuppress）。DIコンテナには`IWorklistWriter`を一切登録しないことで規則5（SCPは読み取り専用）を徹底。CLIに`--db <path>`を追加。`dotnet test`で全体135件成功（Abstractions側MatchEngine単体36件、Store統合7件追加、Scp統合5件追加）。DCMTK `findscu`（独立実装）でSQLiteに実データを1件投入し、①Days Back 60/Forward 2相当の広い範囲要求でもCurrentEntryの1件だけが返る、②範囲外の日付要求で0件+Success、③PatientID不一致で0件+Successを実機さながらに確認。 | 実装指示書のN-02〜N-13/E-05/E-06のID定義は`docs/開発計画_v2.md`欠如のためrepo内に無い（T1と同じ制約）。上記の手動検証・自動テストで「1人モデルで意味のある」正常系・異常系（範囲内/範囲外/境界値/PatientID一致不一致/PatientName一致不一致（カナ・漢字）/Modality strict・lenient・ignore/CurrentEntry無し/ワイド範囲での1件のみ返却）を広くカバーしたが、指示書のID番号との対応表は作っていない。PatientNameのワイルドカード照合は「漢字表記」と「カナ表記」のどちらかに一致すればOKといたシンプルな設計（正式なPN構成規則はT7の仕事なので、そこで見直す前提）。 |
| 2026-09-10 | T6（DatasetBuilder） | ○ 合格。ブランチ `task/T6-dataset-builder`（T5を`main`へマージ後に分岐）。`Ascube.Mwm.Core.Dataset.DatasetBuilder` を実装。source DSL 6種（`db:`/`const:`/`echo:`/`uid:study`/`coalesce:`/`auto:*`）＋`fallback`/`required`/`onMissing`(`empty`/`omit`/`reject-item`)/`maxLength`/`when` を指示書どおり実装し、SQ 入れ子（現行スキーマの2階層まで）にも対応。来歴（Provenance）はタグのDICOM表記文字列→実際に使われたsource式（fallbackが勝てばfallback側）を返す辞書として実装。1件の生成失敗（requiredな要素が解決できない）は例外を投げず`DatasetBuildResult.Suppressed`で報告し、呼び出し側（`MwmDicomService`）はSuppressedの行をyieldせず0件+Successにする（規則2・規則4の合わせ技）。T5でMwmDicomServiceに書いていた暫定の直接マッピングコード（`TryBuildDataset`/`ComposePatientName`/`ToDicomSex`）は全廃してDatasetBuilder呼び出しに置き換えた。追加の受入条件（`PatientSizeM`/`PatientWeightKg`が`null`のとき`(0010,1020)`/`(0010,1030)`がデータセットに現れない、required欠損でSuppressed→0件+Success）をテストと実機同等の手動検証（DCMTK findscu）の両方で確認。`dotnet test`で全体156件成功（Core.Tests に DatasetBuilder 本体10件＋DSL個別テスト11件を追加）。DCMTK `findscu -d`で実データ1件を取得し、①`(0010,1020)`/`(0010,1030)`が丸ごと省略される、②`(0008,0060)`/`(0040,0003)`/`(0040,1001)`など値の無い項目は「タグは存在するが空」（`onMissing:empty`）で出る、③`auto:patientName`/`uid:study`/`db:*`がそれぞれ正しく解決される、ことを確認。 | `coalesce:`と`echo:`は実際のプロファイル（_base/BMD_HOLOGIC系）では未使用（`db:`/`const:`/`auto:*`/`uid:study`のみ使用）。`coalesce:`の区切り文字は指示書に指定が無く独自に`[src1\|src2]`形式（パイプ区切り）を採用した。VM（多値）は現行プロファイルが全てvm=1のため単一値のみ対応（多値対応は未実装、必要になったら追加）。`when`も指示書に具体的な文法の指定が無く、`source`と同じDSL式を評価し非null/非空なら含める、という最小解釈で実装した。いずれも実際の利用例が出た時点で見直すこと。 |
| 2026-09-10 | T7（PnEncoder・文字コード3案） | ○ 合格（コア実装＋DatasetBuilder結線＋DCMTK手動検証まで完了。時間制約下で計画的にスコープを絞った）。ブランチ `task/T7-pn-encoder`（T6を`main`へマージ後に分岐）。`Ascube.Mwm.Core.PatientName` に `HalfToFullWidthKana`（半角→全角カナの自前変換表。濁点/半濁点の2文字→1文字結合、長音・中点対応）、`PnByteValidator`（規則19：fo-dicomの自己再読込に頼らず生バイトを直接検査。ISO_IR13は0x20-0x7E/0xA1-0xDF範囲チェック＋0x5C禁止、ISO_IR192はUTF8Encoding(throwOnInvalidBytes:true)＋U+FFFD禁止）、`PnEncoder`（charset.patientNameのgroup1/2/3割当からPN値を組み立て、生バイト検証に落ちたらSuppressed＝代替文字で無言に化けさせない）を実装。`DeviceProfile.PatientNameGroups`を追加し、`DatasetBuilder`の`auto:patientName`をこの新エンコーダに差し替え（T6の暫定「漢字あれば漢字・無ければカナ」ロジックは全廃）。`dotnet test`で全体181件成功（PatientName関連だけで30件：半角全角変換・生バイト検証・3案でのPN組み立て・外字（髙﨑德）の扱い）。**DCMTKで3案すべてを実機同等に確認**：①(BMD_HOLOGIC, ISO_IR192, group1=kanaFull/group2=kanji/group3=kanaFull)→`タケダ^タロウ=髙﨑^德=タケダ^タロウ`、②(.alt1, group1=none)→`=髙﨑^德=タケダ^タロウ`、④(.alt2, ISO_IR13, kanaHalf単独)→dcmdumpのコンソール表示ではなく**生バイトを16進で直接確認**（`C0 B9 C0 DE 5E C0 DB B3` = 全バイト0xA1-0xDFかASCII範囲内、"ﾀｹﾀﾞ^ﾀﾛｳ"のJIS X0201単独表現と一致。CP932漢字の先頭バイト0x81-0x9F/0xE0-0xEFは一切出現しない）。 | **姓名の区切りルール（区切りが無ければ全体を姓、3つ以上なら先頭を姓・残りを結合して名）は未実装**：WorkItemViewが最初からFamily/Given分割済みで渡ってくるため、このSCPの層では出番が無いと判断（BRIDGE-Navi側の依頼電文パース時点の話であり、このSCPのスコープ外という解釈）。ローマ字群（romaji）はWorkItemViewにローマ字表記の列が無いため未実装（常に未解決＝その群は省略）。**`mwm-scu preview`CLIは未実装**（T11で作る予定のツールにT7の受入条件が依存しているため、代わりにSCPを実際に起動してfindscu+生バイトのhexdumpで検証した）。dcmdumpの`+U8`オプションはこの環境のDCMTKビルドでISO_IR13→UTF8変換時に警告を出したが（"the value of SpecificCharacterSet is not updated"）、生バイトの16進解析で独立に正しさを確認済みなのでDCMTK側のツール制約と判断した。時間制約（PCの八王子ベース移設のため約1時間で安全に区切る必要があった）により、`(0010,1020)`等の他フィールドとの往復チェックや3案の完全な自動化されたバイト比較テスト（今回は手動）は次回以降に持ち越し。 |
| 2026-09-10 | T8（監査＋explain-query） | ○ 合格。ブランチ `task/T8-audit-explain`（T7を`main`へマージ後に分岐。移設予定は延期・開発続行）。監査ログ（`AuditCFind`/`AuditCFindItem`）を**ワークリストDBとは物理的に別ファイル**（`Ascube.Mwm.Store.Audit`、`SqliteAuditStore`）として実装した。**設計判断**：規則5「SCPはSQLiteを読み取り専用で開く。書き込む経路を作らない」は文字どおりには監査ログの書き込みと矛盾するが、これはCurrentEntry/WorkItem/Patient/UidAllocation（BRIDGE-Navi管理下のワークリストデータ）を保護する規則であり、SCP自身が生成する運用ログである監査DBはSCPが書く以外に主体がいないと解釈。ファイルを分けることで「ワークリストDBへの書き込み経路が物理的に存在しない」を規則5の文字どおりの意味でも担保した（`Program.cs`は`IWorklistWriter`を一切DI登録しない）。`MwmDicomService.OnCFindRequestAsync`をtry/finallyで包み、要求データセットをDICOM JSON Model（`DicomJson.ConvertDicomToJson`）で記録、候補ごとの来歴（Provenance）またはSuppressed理由、0件時の説明文（`IWorklistRepository.ExplainAsync`のT5実装を再利用しつつ、Modality不一致・DatasetBuilder Suppressedの2ケースはSCP層で追加）、所要時間、PeerAborted（finally到達前に切断された場合の簡易推定）を記録するようにした。`mwm-admin admin explain-query --run <id> [--audit-db <path>]`を追加し、RunId・要求条件・0件理由（または来歴）・要求データセット全文を表示する。`dotnet test`で全体191件成功（Store.Testsに監査DB単体6件追加、Scp.Testsに監査記録の結合4件追加）。**DCMTKで実機同等の一連の流れを確認**：①findscuで範囲外日付・PatientID不一致・正常一致の3パターンを実行→`explain-query`でそれぞれ「ScheduledDateが要求範囲外です（要求:.../保持:...）」「PatientIDの指名が一致しません（要求:.../保持:...）」「1件返しました＋候補の来歴一覧」を確認（実装指示書T8の受入条件1〜4を実機データで実証）。理由5（DatasetBuilder Suppressed）はユニットテストで確認済み（`Find_PatientNameMissing_RecordsSuppressedReasonOnItem`）。 | AuditCFindの`Clamped`列は1人モデルでは常にfalse（日付クランプ機能自体を作っていないため。将来T5の設計を見直す場合に備えて列だけ用意）。`PeerAborted`の検出は「finallyに到達する前に例外／早期dispose が起きたか」という簡易な signal であり、真に「相手が切断した」ことの確定的な検出ではない（DicomServiceのOnConnectionClosedとは別経路。より正確にしたければT9のraw stream recorderと連携する必要があるが、今回はスコープ外とした）。監査ログの保持期間・ローテーション・サイズ上限は未実装（無制限に増え続ける。運用タスクT12あたりで検討）。 |
| 2026-09-10 | T9（raw stream recorder） | ○ 合格。ブランチ `task/T9-raw-recorder`（T8を`main`へマージ後に分岐）。T0/S-3スパイクで確定済みの方式（②: `INetworkManager`/`INetworkStream` decorator）をそのまま本実装に移植した。`Ascube.Mwm.Scp.Capture`に`RecordingNetworkManager`/`RecordingNetworkStream`/`TeeStream`（`DesktopNetworkManager`をラップし`AsStream()`のRead/Writeを記録。fo-dicom本体は無改変・規則11）と`RawCaptureWriter`（bounded channel経由で固定ヘッダ＋生バイトの追記専用バイナリ＋index JSONLを書く。日次／容量ロールオーバー時にSHA-256サイドカーを書く）を実装。`Program.cs`で`AddFellowOakDicom()`の直後に`AddNetworkManager<RecordingNetworkManager>()`を呼ぶ順序を厳守。出力先は`.gitignore`済みの`captures/`（規則14）。`--no-capture`で無効化可能。`dotnet test`で全体198件成功（RawCaptureWriter単体7件）。**手動検証で実装上のバグを1件発見・修正**：`BoundedChannelFullMode.DropWrite`は`TryWrite`が常に`true`を返してしまい、欠落検知ができないことが発覚（テストで検出）→ 既定の`Wait`モード＋`TryWrite`の非ブロッキングfalse復帰を使う方式に変更して解決。もう1件：チャンクごとの`Flush()`を入れていなかったため、プロセスが強制終了された場合に直近のキャプチャファイルが空のまま残ることを実機確認で発見→ チャンク書き込みごとに`Flush()`する方式に変更（このSCPの通信量ではボトルネックにならない）。DCMTKで実際にecho/findを実行し、生バイトの16進を直接確認：AEタイトル`ASCUBE_MWM`/`TEST_SCU`が生バイトのまま複数箇所に現れることを確認（T0/S-3スパイクと同じ検証方法）。 | 容量ロールオーバーの閾値（既定200MB）・保持期間・古いキャプチャの自動削除は未実装（無制限に増え続ける。T12で検討）。association ID はTCP接続確立時点で採番する乱数のため、DicomAssociationのCalling/Called AEとの対応付けは記録時刻とSCPログ（`ScpHostedService`のログにCalling/Called AEが出る）を突き合わせる必要がある（ネットワーク層でのフック地点がA-ASSOCIATE-RQパース前のため、設計上の制約。T0/S-3スパイクと同じ）。強制終了（kill -9相当）時、書き込み中の最後のファイルはデータ自体は残るがSHA-256サイドカーが作られない（正常終了時のみ計算する設計）。 |
| 2026-09-10 | T10（ホットリロード＋自動ロールバック） | ○ 合格。ブランチ `task/T10-hot-reload`（T9を`main`へマージ後に分岐）。`Ascube.Mwm.Core.Config.LiveProfileRegistry`（volatile参照の入れ替えのみで読み書き。`TryReload`は対象プロファイルIDを全て再読込・再検証し、1つでも失敗したら何も差し替えない＝自動ロールバック）と`Ascube.Mwm.Scp.ProfileHotReloadService`（`FileSystemWatcher`＋500msデバウンス。成功時は`config/backup/<id>.<timestamp>.jsonc`へ差し替え前の内容を自動バックアップし、`ProfileDiffFormatter`で旧→新の差分をログ出力）を実装。**「進行中のアソシエーションは読込時のスナップショットを使い続ける」を保証する設計**：`ScpRoutingContext`はプロファイルの固定リストではなく`LiveProfileRegistry`への参照を持ち、`MwmDicomService`は`OnReceiveAssociationRequestAsync`で1回だけ`ResolveByCalledAe`して結果を`_resolvedProfile`にキャッシュする（T3から既にあった設計）ため、同一アソシエーション内で登録内容が変わっても影響を受けない。この性質をテストで直接確認：1つのアソシエーション内で2件のC-FINDを送り、1件目の応答受信直後（＝2件目送信前）にレジストリを裏で差し替えても、2件目の応答は差し替え前の設定のままであることを検証。`dotnet test`で全体205件成功（LiveProfileRegistry単体4件、Scp統合5件）。**現地運用スクリプト`scripts/switch-charset.ps1`を追加**（案①②④の`charset`ブロックを正規表現で置換して保存するだけの薄いスクリプト。9/28に文字コード案を現地で切り替える手順として`scripts/README.md`に使い方を記載）。実機同等の手動検証：`mwm-scp`を起動したまま`BMD_HOLOGIC.jsonc`の`charset`を案①→案④へ書き換えて保存→ログに「再読込に成功しました」「差し替え前の設定をバックアップしました」「差分」が出力され、次のfindscuの応答が`SpecificCharacterSet=ISO_IR 13`に切り替わることを確認。検証失敗（ISO 2022系を仕込む）でも稼働中の設定が維持されることも確認。 | ホットリロードが差し替えるのは起動時に`--profile`で渡したプロファイルIDの中身のみ（`network.port`や`network.aeTitle`を変えても待受ポート自体は再構成されない。ポート変更にはプロセス再起動が必要、という制約を`scripts/README.md`に明記）。`switch-charset.ps1`は`Write-Host`の日本語表示がコンソールのコードページ次第で文字化けすることがあるが、ファイルの中身自体は正しく書き換わることを確認済み（規則どおり「コンソール表示では判定しない」の方針に従い、判定はファイル内容と`mwm-admin admin validate`で行った）。差分ログは`DeviceProfile.Raw`の行単位集合差分という簡易な方式（真の構造比較ではないため、順序だけが変わるような編集でも差分ありと表示されうる）。 |
| 2026-09-10 | T11（mwm-scu / mwm-admin CLI） | ○ 合格。ブランチ `task/T11-cli-tools`（T10を`main`へマージ後に分岐）。`Ascube.Mwm.Tools`を`Program.cs`＋`Commands/`配下の各コマンドクラスに再構成し、`scu echo`/`scu find`（`--emulate-apex-defaults`対応）/`scu preview`/`scu replay --from-audit`/`admin validate`（T1から移設）/`admin explain-query`（T8から移設）/`admin health`/`admin export-support-bundle`を実装。**`scu find --hard-close-after N --close-mode fin\|rst`**（指示書が「DicomClientでは実現できない」と明記する最難関の機能）は、`DicomClient`をそのまま使いつつ、T9と同じDI差し替え手法（`INetworkManager`のクライアント接続生成経路で`System.Net.Sockets.NetworkStream.Socket`を捕まえる`SocketCapturingNetworkManager`）でfo-dicomを一切改変せずに実現した（規則11）。N件目のPending応答受信直後のコールバック内で`LingerState=(true,0)+Close()`（RST）または通常の`Close()`（FIN）を呼び、A-RELEASEを送らずTCPを切断する。`scu preview`は`WorkItemView`を`WorkItem`/`Patient`テーブルの履歴から引く`SqliteWorklistRepository.GetLatestByPatientIdAsync`を新設し、`DatasetBuilder`で組み立てたデータセットをdcmdump形式（来歴つき）＋実際にシリアライズした`.dcm`ファイルの16進ダンプで表示する。`admin health`は`mwm-scp`プロセスとHTTP等で通信せず、TCPポートへの疎通プローブ＋ワークリストDB（`SqliteWorklistRepository.GetCurrentStatusAsync`を新設）＋監査DB（`IAuditReader.GetLatestCFindAsync`を新設）を直接読んで「待受状態／現在の受診者の有無とTTL残／直近のC-FIND」を表示する。`admin export-support-bundle`はプロファイル・直近N件の監査記録（JSON）・キャプチャファイル一覧（生バイトは含めない）を1つのzipにまとめる。`dotnet test`で全体217件成功（新設`Ascube.Mwm.Tools.Tests`にSocket捕獲・強制切断の単体テスト4件を追加）。**実機同等の手動検証で全コマンドを確認**：`scu echo`/`scu find`/`--emulate-apex-defaults`/`--date today`が正常応答、**`--hard-close-after 1 --close-mode rst`と`--close-mode fin`の両方でSCPが生き残り直後のechoが通ることを確認**（E-02/E-03相当）、`scu preview`はDCMTK dcmdumpでも独立に読めることを確認、`scu replay --from-audit`は監査ログのRequestJsonから実際に再送できることを確認、`admin health`/`admin export-support-bundle`とも意図どおりの出力。**手動検証で実装バグを1件発見・修正**：`scu preview`の16進表示が`DicomDataset`の内部バッファ（符号化前の生の文字列表現）を直接読んでいたため全角文字が`3f`（'?'）に化けていた。`DicomFile.Save()`で実際にシリアライズしたバイト列を読み直す方式に変更し、UTF-8の正しいマルチバイト列（髙=e9 ab 99等）が出ることを確認した。 | `scu preview`はSOPClassUID/SOPInstanceUIDを持たないワークリスト応答データセットを`.dcm`ファイル化するため、プレビュー専用の仮UIDを補っている（実際のワイヤ上のP-DATA-TFにはこの2タグは乗らない。ファイルはdcmdump等での確認用の副産物と明記）。`admin export-support-bundle`は生キャプチャファイル（`.bin`）そのものはzipに含めない設計とした（サイズと受診者データ由来のリスクを避けるため。一覧とSHA256だけを含める）。`R-SCU-01〜07`という受入条件のID自体は`docs/開発計画_v2.md`欠如のためrepo内で確認できず、実装指示書本文に書かれた機能要件（表の9コマンド＋`--hard-close-after`の警告文）を全て満たす形で実装したことをもって合格とみなした。 |
| 2026-09-10 | T12（Windowsサービス化・運用スクリプト） | △ 部分合格。ブランチ `task/T12-service-ops`（T11を`main`へマージ後に分岐）。`scripts/`に`00_watch.ps1`（5秒間隔。**指示書どおり「現在の受診者：あり／なし、TTL残 mm:ss」を画面上部に大きく表示**）、`01_health.ps1`〜`05_explain.ps1`（単発確認用の薄いラッパー）、`mark.bat`（`echoscu -aet MARK-STEP<N>`）、`start-capture.bat`（dumpcapのリングバッファ。`-b filesize:102400 -b files:100`・フィルタ`tcp port 11112 or icmp`）、`collect-evidence.ps1`（証跡一式を1フォルダに集約）、`install-service.ps1`/`rollback.ps1`（Windowsサービス登録・異常終了時の自動復帰設定・ロールバック）を実装した。**手動検証で全スクリプトを実際に実行して確認**（`install-service.ps1`/`rollback.ps1`を除く。下記理由）。**手動検証で重大なバグを2件発見・修正**：①`mark.bat`/`start-capture.bat`にJapanese REMコメントを入れていたところ`cmd.exe`がバッチファイルの解析自体に失敗した（.batは.ps1と違いUTF-8で日本語コメントを書くと壊れる。英語コメントに書き換え、詳細な説明は本README側に日本語で書く方式に変更）。②`00_watch.ps1`が「現在の受診者：あり」を正しく検出できず常に「なし」と表示していた。原因は、`mwm-admin`（.NET、UTF-8出力）の子プロセス出力をPowerShell変数に取り込む際、Windows PowerShell 5.1の既定エンコーディングでは正しくデコードされず、正規表現が一致しなかったため。`[Console]::OutputEncoding = [System.Text.Encoding]::UTF8`を明示設定して解決した（**これは9/28に画面を見て真っ先に確認するはずの項目が常に誤表示になるという、実機接続テスト当日に直結する重大な不具合**で、実際に動かして確認しなければ気づけなかった）。 | **`install-service.ps1`/`rollback.ps1`は実行していない。** システム設定の変更（Windowsサービス登録・起動・ACL変更）を伴う操作であり、開発セッションでの自律実行を避けた（要ユーザー許可）。スクリプトの静的な内容（publish→バックアップ→サービス登録→`sc.exe failure`での自動復帰設定、および復元手順）は書いたが、受入条件（強制終了→自動復帰、rollbackが5分以内）は実機のサービス登録なしには検証できない。**実機での実行前に必ずユーザーの確認を得ること。** サービスアカウントは既定でLocalSystemとした（専用アカウント+ACLは運用ポリシー次第で`-ServiceAccount`パラメータから設定できるようにしたが、パスワード管理を伴う`-Credential`部分は簡略化のため未実装）。`00_watch.ps1`以外のスクリプト（01〜05・mark.bat）は`mwm-admin`/`mwm-scu`の出力をPowerShell側で解析せずそのまま画面に流すだけなので、上記のエンコーディング問題は発生しない。 |
| 2026-09-10 | T13（社内テスト自動化） | ○ 合格（「絶対に削らない」5項目すべて自動テストで固定。次点5項目は既存テストで確認済み。wlmscpfs意味比較・性能計測は実施したが正式な数値目標が repo 内に無いため詳細は残課題欄）。ブランチ `task/T13-integration-tests`（T12を`main`へマージ後に分岐）。着手前に既存カバレッジを調査した結果、E-01・T7の3案（手動のみ）・W-01/W-02（DB層のみ）・E-02/E-03（アソシエーション確立前の切断のみ）に実質的なギャップがあることが判明したため、以下4点を追加した。**(1) W-01/W-02の実配線テスト**：`PatientContinuityTests.cs`。Store.Tests は `SqliteWorklistWriter`/`SqliteWorklistRepository` 単体、Scp.Tests の `CFindMinimalTests` は `FakeWorklistRepository` までしか見ていなかったため、実際の `SqliteWorklistWriter`（BRIDGE-Navi側）→実SQLiteファイル→`SqliteWorklistRepository`（SCP側）→実 `DicomClient` による C-FIND、という配線全体を1本で通し、規則16（1人モデル・患者取り違え防止）を実配線で検証する。**(2) E-02/E-03「応答送信中」の強制切断**：`MidResponseDisconnectTests.cs`。既存の `AbruptDisconnectTests`（T3）はアソシエーション確立前の切断のみを検証しており、規則3が本来指す「C-FINDのPending応答受信後・Success送信前」の切断は未検証だった。T11の `SocketCapturingNetworkManager`（`scu find --hard-close-after` と同じ仕組み）をテストにリンクして再利用し、FIN・RST双方で検証、SCPが生き残り次のアソシエーションが通ることを確認。**(3) T7の3案のDCMTK自動検証**：`DcmtkCharsetVerificationTests.cs`。T7では手動でDCMTK確認していたが自動テストが無かった。`findscu -X`で応答を`.dcm`として抽出し`dcmdump`（別実装）で読めることを検証、加えて案④（ISO_IR13単独）はPatientName要素の生バイトだけをExplicit VR短形式のタグ境界から直接切り出し、JIS X0201範囲（0x20-0x7E/0xA1-0xDF）外バイトが無いことを確認（規則19：fo-dicomのdecoderに頼らない）。DCMTKがPATHに無い環境ではこのテストは例外で失敗する設計（黙ってスキップしない）。**(4) wlmscpfs意味比較**：`scripts/06_wlmscpfs_compare.ps1`。実機の`mwm-scp`と、DCMTK純正の参照実装`wlmscpfs`に同一内容の受診者データを与え、同じ返却キーで`findscu`→`dcmdump`し、UID・時刻等の可変値をマスクしてから差分比較する。実装中に**wlmscpfs自体のドキュメント未記載の挙動を3点発見**（Called AE Titleをディレクトリ名として解釈する、AE名と同名のサブディレクトリ内に空の`lockfile`を事前作成しないと起動時に読めない、Type1相当の属性（ScheduledProcedureStepStartTime/ScheduledProcedureStepID）に実値が無いファイルは黙って無視される）。これらを踏まえた実装で、実際に起動した`mwm-scp`（現在の受診者なし＝0件）に対してスクリプトを実行し、wlmscpfs側の1件との差分が正しく検出されることまで確認した（一致ケース自体は同じdiffロジックのため未実施）。`dotnet test`で全体224件成功（Scp.Testsが34→41件に増加）。 | **wlmscpfs意味比較の「一致するケース」は実データ一致の実行までは確認していない**（診断ロジックは同一なので低リスクと判断したが、実際に`mwm-scp`へ`SetCurrentAsync`で同一データを投入した一致確認は9/28に向けて一度実施すること）。**性能計測（§7-4）の正式な数値目標は`docs/開発計画_v2.md`欠如によりrepo内で確認できない**（T1/T5と同じ既知の制約）。参考として実測：DCMTK `echoscu`/`findscu`を外部プロセスとして20回連続実行した所要時間の平均は C-ECHO 148ms/回、C-FIND(0件) 174ms/回（プロセス起動オーバーヘッド込み。SCP単体のDIMSE処理時間そのものではない）。正式な受入値が無いためこの行はN/A・実測値のみ記録という扱いとする。**L-01（24時間稼働試験）は未実施**：実施計画上は9/17夜開始・9/18確認だが、実際の開発は9/10時点で前倒しで進んでおり、このセッション内で実時間24時間を待機することはできない。9/17に近い時期に改めて実施すること（`install-service.ps1`実行後、実機サービスとして稼働させた状態で行うのが望ましい）。**次点5項目（N-16・E-11・L-03・E-13/E-14）は新規テストは追加していない**：調査の結果、既存テスト（T4/T6/T8/T2/T10それぞれの回で追加済み）で実質的にカバー済みと判断したため（詳細はソース：`CFindMinimalTests.cs`のSQ構造・Suppressedテスト、`ConcurrencyTests.cs`、`LiveProfileRegistryTests.cs`/`HotReloadIntegrationTests.cs`）。E-15（サービス強制終了からの自動復帰）はT12から引き続き未検証（`install-service.ps1`が実機実行されていないため）。 |
| 2026-09-10 | T14（横展開の検証） | ○ 合格（N-20：コード変更ゼロ）。ブランチ `task/T14-es-default-verification`（T13を`main`へマージ後に分岐）。それまで枠のみだった `config/profiles/ES_DEFAULT.jsonc` に、架空の内視鏡プロファイルとして charset・matching（`modalityMatching: strict`）・dataset.elements（BMD_HOLOGIC相当から身長体重を除いたもの。(0040,0100)[0].(0008,0060) を `const:ES` に設定）を書いた。**変更したファイルはこの1枚のみ**（`git status --short` で `config/profiles/ES_DEFAULT.jsonc` 以外に差分が無いことを確認）。実機同等の手動検証：専用の使い捨て投入プログラム（`SqliteWorklistWriter` を直接呼ぶだけ。ascube-mwm 本体のコードは一切変更していない）でCurrentEntryを1件投入し、`mwm-scp --profile ES_DEFAULT`（ポート11113・AE=ASCUBE_ES）を実際に起動、DCMTK `findscu -k "0040,0100[0].0008,0060=ES"`（別実装）で1件のPending→Successを確認、さらに `-k "...=US"`（別Modality）で0件+Successになることも確認し、`matching.modalityMatching: strict` が実際に効いていることを裏付けた。`dotnet test`で全体224件成功（ES_DEFAULTは設定ファイルのみのためテスト件数に変化なし）。**実装指示書が例示する `findscu -k Modality=ES`（トップレベルキー）ではなく `-k "0040,0100[0].0008,0060=ES"`（SQ内キー）で検証した**：`QueryCriteriaParser.Parse`（T5）はDICOM規格どおりModalityを `ScheduledProcedureStepSequence[0].Modality` から読み取る実装になっており、トップレベルの `(0008,0060)` は読んでいないため。 | 実運用で内視鏡4台を本当に横展開する場合の装置ごとの個体差（AE Title・ポート・型番固有のデータセット要素）はこの1枚には反映していない（あくまで「設定ファイルだけで新しい装置種別を追加できる」という設計の骨格検証）。`Ascube.Mwm.Store.MwmStoreOptions.DeviceProfileId` が `mwm-scp` 起動時の先頭 `--profile` 引数に固定される実装（T2・Program.cs）のため、複数装置を**1つの`mwm-scp`プロセスに`--profile`を複数指定して**混在させる場合は現状 DeviceProfileId のスコープが先頭のものに寄ってしまう。今回の検証は BMD_HOLOGIC とは別プロセス・別ポートで独立起動する構成（実運用でも装置ごとに別プロセスを想定）で行ったためこの制約には触れていないが、複数`--profile`を1プロセスに混在させる設計を将来使う場合は見直しが必要な点として記録しておく。 |
| 2026-09-11 | リリース前チェック（SBOM／サードパーティ表示） | ○ 合格。ブランチ `task/release-hygiene-sbom-logging`（T14を`main`へマージ後に分岐）。CLAUDE.md 規則13「リリースごとに `docs/sbom.json` と `docs/THIRD-PARTY-NOTICES.txt` を更新する」が未着手だった（両ファイルとも repo に存在しなかった）ため、`dotnet list <project>.csproj package --include-transitive`（Ascube.Mwm.Scp・Ascube.Mwm.Tools の両方、net10.0）の実出力を元に新規作成した。ライセンスは各パッケージのNuGetページで確認：fo-dicom=MS-PL、Microsoft.Extensions.*／Microsoft.Data.Sqlite系／CommunityToolkit.HighPerformance／System.*=MIT、SQLitePCLRaw.*／xunit=Apache-2.0。Directory.Packages.propsに版だけ固定されていて実際にはどのcsprojからも参照されていない Serilog／Serilog.Sinks.File（T3で配線見送り）も「pinnedButNotReferenced」として明記した。あわせて `docs/TODO.md`「本番リリース前にやること」の `logDataPdusを通常運用ではfalseへ` を調査：`IDicomServerFactory.Create<T>` の `configure` コールバックは `DicomServerOptions`（`MaxClientsAllowed`のみ）を受け取り `DicomServiceOptions`（`LogDataPDUs`/`LogDimseDatasets`）を上書きする手段がこの版のAPIには無いことをリフレクションで確認した上で、`new DicomServiceOptions()` の既定値自体が両方 `false` であることもリフレクションで直接確認（fo-dicom 5.2.6）。**配線を無理に足すより「既定値で安全側に倒れていることを確認した」という記録を残す方針にした**（コード変更なし。`git diff src/` は空）。`dotnet test` で全体224件成功（変化なし。ドキュメントのみの追加）。 | ライセンス表記は目視・NuGetページ確認によるもので、SPDX/CycloneDX等の標準SBOM形式ではなく独自の軽量JSON。将来 `dotnet-project-licenses` 等のツール導入で自動生成に置き換えることを検討してもよいが、今回はDCMTK同様「外部ツールを追加導入しない」方針を優先し手作業で作成した。Calling AE照合のignore→strict切替（TODO.md残項目）はHologic側の実AE Title確定待ち（§8の社外確認、締切9/11）のため今回は着手していない。 |
| 2026-09-14 | CI（GitHub Actions） | ○ 緑。windows-latest / .NET 10 / Release で restore→build→test、.trx を artifact 保存。DcmtkCharsetVerificationTests は DCMTK 非搭載のため --filter で除外（⚠ 文字コードの規格適合はCI対象外。開発機でのDCMTK実行が引き続き正本）。docs/**・*.md のみの push はスキップ。 | 第2弾で複数人が触るようになったら DCMTK の CI 導入を再検討 |
| 2026-09-16 | admin set-current / admin clear-current | ○ 合格。ブランチ `task/admin-set-clear-current`。docs/連携テスト手順.md §2「受診者を投入する手段が無い問題」への対応。`Ascube.Mwm.Tools` に `AdminSetCurrentCommand`/`AdminClearCurrentCommand` を追加し、`SqliteWorklistWriter.SetCurrentAsync`/`ClearCurrentAsync` をCLIから直接叩けるようにした（本体コードは同じ経路を通る薄いラッパー）。`--patient-id`/`--scheduled-date`/`--profile` が必須（`WorklistEntry` の `required` に合わせた）、他は任意。プロファイルIDは `ProfileLoader.LoadAndValidate` で存在確認してから書き込む（誤字を書き込み後ではなく書き込み前に検出）。`--patient-id` が12桁数字でない場合は警告を出す（規則18：項番3/項番4取り違えの機械的検知）。`Ascube.Mwm.Tools.Tests` に6件追加（値の反映・必須引数不足・未知プロファイル・同一受診者への複数回呼び出しでUID不変・Clear後にGetCurrentAsyncがnull・Clearの必須引数不足）。`dotnet test` で全体227件成功。実際にSCPを起動した状態でCLIから2人分（武田太郎／鈴木花子）を投入→`scu find`で1件ずつ正しく返る→`admin clear-current`で0件+Successに戻ることを手動でも確認。`scripts/07_set-current.ps1`/`08_clear-current.ps1`を追加し`scripts/README.md`に使い方を記載（既存の00〜06と同じ`-ToolsExe`切替パターン）。 | このセッションでの使い捨て投入プログラム（scratchpad配下）はこのコマンドで役目を終えたため削除してよい。9/28当日、BRIDGE-Naviが不調なときの切り分け手段として`00_watch.ps1`と併用する想定。 |
| 2026-09-17 | プロファイルの currentTtlMinutes をTTL計算へ配線 | ○ 合格。同ブランチ続き。`config/profiles/*.jsonc` の `visibility.currentTtlMinutes` は `DeviceProfile.CurrentTtlMinutes` として読み込まれ検証もされていたが、実際のTTL計算（`SqliteWorklistWriter` の `MwmStoreOptions.CurrentTtl`）には一切反映されていなかった（既定15分固定）。`docs/連携テスト手順.md` S3-4「currentTtlMinutesを1に書き換えてホットリロードで反映」の手順が実際には動かない状態だったと判明。`AdminSetCurrentCommand` でプロファイル読込後に `CurrentTtl = TimeSpan.FromMinutes(profile.CurrentTtlMinutes)` を明示的に渡すよう修正。BRIDGE-Navi本体は `Ascube.Mwm.Core` を参照できないため、この配線が効くのは `admin set-current` 経由の場合のみ（本番のBRIDGE-Naviは引き続き自分でCurrentTtlを指定する設計）。テスト1件追加（`currentTtlMinutes: 1000`の専用プロファイルで900分以上残っていることを確認。既定15分のままなら確実に失敗する作り）。`dotnet test`で全体228件成功。実機同等のCLI手動検証でも既定15分プロファイルが正しく動くことを確認。 | なし |
| 2026-09-17 | 身長の単位をcmに統一（PatientSizeM→PatientHeightCm） | ○ 合格。同ブランチ続き。`docs/APEX 5.6 DICOM Conformance Statement_0 1.pdf` を一次情報として確認した際、`(0010,1020)` Patient's SizeはDICOM PS3.3上メートル単位である一方、BRIDGE-Navi側・依頼電文はいずれもセンチメートル表記と判明。旧設計（`docs/実装指示書.md`等の疑似コード）はBRIDGE-Navi側で`/100.0`変換する想定だったが、変換漏れ・二重変換をascube-mwm側で検知できない問題があるため、**変換責務をascube-mwm側に一元化**する設計変更を実施（`docs/設計変更メモ_v2.1.md` §I）。`Ascube.Mwm.Abstractions.WorklistEntry`/`WorkItemView`の`PatientSizeM`（メートル前提）を`PatientHeightCm`（cm）に改名し、`Ascube.Mwm.Core.Dataset.DatasetBuilder`の`db:PatientHeightCm`解決時に`/100.0`変換する処理を追加。SQLite列名・`admin set-current --height-m`→`--height-cm`・`config/profiles/_base.jsonc`のsource式も追随。テスト1件追加（165cm→DICOM `(0010,1020)`が"1.65"になることを確認）＋既存テストのデータをcmベースに修正。`dotnet test`で全体229件成功。実機同等の手動検証（SCP起動→`admin set-current --height-cm 165`→`scu find`）で`(0010,1020)`が`1.65`として送信されることを確認。 | 既存の開発用DB（`data/*.db`）はスキーマ変更のため削除して作り直した（実データではないため影響なし）。BRIDGE-Navi側の実装はまだ無いため、本番でこの契約が守られるかはBRIDGE-Navi結線時（第3段）に要再確認。 |
| 2026-09-17 | FIX-001 DB未作成時のC-FINDが例外を投げていた件を修正 | ○ 合格。ブランチ `fix/FIX-001-missing-db-returns-empty`。武田病院健診センター現地PC（BRIDGE-CUBE）での実機確認により発見：`data\mwm.db`が存在しない状態（＝当日まだ誰もSetCurrentAsyncしていない）でC-FINDを受けると`SqliteWorklistRepository`が`OpenAsync`で`SqliteException`（Error 14: unable to open database file）を投げ、アソシエーションが切断されていた（規則2違反）。`TryGetLiveViewAsync`/`ExplainAsync`/`GetCurrentStatusAsync`/`GetLatestByPatientIdAsync`の各エントリで`OpenAsync`前に`File.Exists`を確認し、DB未作成時は例外を投げず「受診者なし」として空を返すよう修正（ファイル不在以外の開けない理由は従来どおり例外を上へ投げる。テストで検証：ファイルを排他ロックした状態でも`SqliteException`が伝播することを確認）。`AdminHealthCommand`（`admin health`/`01_health.ps1`）もDB未作成を異常ではなく「現在の受診者：なし」と表示するよう修正。受入条件を先にテスト化し、修正前に実機と同じ症状（Store.Tests側は`SqliteException`、Scp.Tests側は`ConnectionClosedPrematurelyException`）で失敗することを確認してから実装した。テスト4本追加（Store.Tests）＋1本追加（Scp.Tests、`SqliteWorklistRepository`を実際に使いDICOM協会レベルでアソシエーション維持＋Success応答を検証）。`dotnet test`で全体237件成功（既存232件＋新規5件、退行なし）。 | §4-2前半（SCP起動時にdataディレクトリと空DBを自動生成）は「推奨（無くても動く）」のため機能凍結前の今回は見送り。9/28前に現地PCで`scripts/03_echo.ps1`→`04_find.ps1`（0件+Success）→`01_health.ps1`（現在の受診者：なし）→`07_set-current.ps1`→`04_find.ps1`（1件+Success）の手動確認を実施すること（FIX-001.md §8）。 |
