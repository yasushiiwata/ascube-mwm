# 9/28 現地接続テスト手順（武田病院健診センター・Hologic APEX 5.6）

| | |
|---|---|
| 版 | v1.1 / 2026-09-17（初版からの見直し：TODOの優先度整理・9項目チェックリストを本文に転記・完了条件を追加） |
| 対象日 | **2026-09-28（月）** |
| 対象装置 | Hologic APEX 5.6（骨塩定量装置・駅前）。Modality Worklistオプションは有効化済み（確認済み） |
| 実施者 | 岩田・（大徳君同席の可能性あり） |
| 上位文書 | `CLAUDE.md` / `docs/実装指示書.md` / `docs/設計変更メモ_v2.1.md` / `docs/依頼電文マッピング表_v1.1.md` / `docs/連携テスト手順.md`（9/16実施の大徳君とのリハーサル） |
| 一次資料 | `docs/APEX 5.6 DICOM Conformance Statement_0 1.pdf`（Hologic公式） |

---

## 0. 今日の目的

> **実機Hologic APEXが、mwm-scpに対して実際にC-FINDを発行し、BRIDGE-Naviがセットした受診者1人が正しくAPEX画面に表示されること。**

これまでの開発機での確認（`docs/連携テスト手順.md`）は、自作の `mwm-scu`（ダミー装置役）で代用してきました。**今日初めて、本物の装置が相手になります。** ダミーでは再現できなかった実機固有の挙動（クエリのタイミング、画面表示、文字コードの実際の解釈）がここで初めて分かります。

⚠ **「0件」は正常応答です。** 該当者が特定できないときは0件＋Successを返します（規則16）。装置側に何も表示されなくても、まず「エラーが出ていないか」「Successで返っているか」を`admin explain-query`で確認してください。

---

## 1. 前日までに終わらせること

### 1-0. 優先度順TODO（2026-09-17時点）

🔴 最優先（社外の回答待ち。詳細は1-1）
- [ ] ヘルゼア：骨密度の検査識別コードCCC3桁
- [ ] Hologic（水端様）：装置設定12項目
- [ ] Hologic（水端様）：Patient Heightのマッピングを `(0010,1020)` に明示設定してもらう（既定None）
- [ ] Hologic（水端様）：Calling AE Title（APEXが名乗ってくる名前）の確認
- [ ] 病院情報システム：ネットワーク・FW承認、**パケットキャプチャ承認**

🟠 大徳君（BRIDGE-Navi）側 — 特に1点目は今すぐ伝える
- [ ] **破壊的変更の周知：`WorklistEntry.PatientSizeM` は `PatientHeightCm` に改名済み（単位もm→cmに変更）。** 大徳君が既に `PatientSizeM` 前提でコードを書き始めていたら、9/28直前にビルドエラーか単位バグで発覚する。今日中に連絡すること（`設計変更メモ_v2.1.md §I`）
- [ ] `SetCurrentAsync`/`ClearCurrentAsync` の結線状況
- [ ] `ClearCurrentAsync` が画面遷移の**全分岐**（正常終了・中断・スキップ・強制終了）から呼ばれるか
- [ ] BRIDGE-Naviの `<TargetFramework>` 確認（`net10.0` でないと `ProjectReference` が通らない）

🟡 現地準備（詳細は1-2〜1-4）
- [ ] mwm-scp＋BRIDGE-Navi同居PCの用意、2LAN構成、IPフォワーディング無効化の確認
- [ ] DCMTK（findscu/dcmdump）・dumpcapの用意
- [ ] インターネット・Claude Code・大型ディスプレイの用意

🟢 9/28前に準備できれば片付けたいもの
- [ ] Calling AE照合の `strict` 化（Calling AE Title確認後。プロファイル編集＋`admin validate`だけで済む軽い作業）

⚪ 9/28当日にしか決まらないもの
- 文字コードの本番確定（①→②→④の順で現地判断）
- Procedure Code欄 `(0040,0009)` を追加送信するか（ヘルゼアのCCC確定と合わせて判断）
- `wlmscpfs` 一致ケースの確認 — **9/28のS2が通ればそのまま埋まる**（差分検出側のみ既に確認済み。追加作業ではない）

🔵 9/28より後でよい（§6「当日は行わないこと」と対応。前倒しで無理にやらない）
- `install-service.ps1` の実機実行（未実施。要ユーザー承認・管理者権限）
- L-01（24時間連続稼働試験）
- E-15（サービス強制終了からの自動復帰）— `install-service.ps1` 実行が前提

---

### 1-1. 社外確認事項（`docs/実装指示書.md §8` の最新版）

| 宛先 | 内容 | 状態 |
|---|---|---|
| **ヘルゼア（富士フイルムメディカル）** | 骨密度の検査識別コードCCC3桁 | ⚠ 未解決なら暫定コード運用（`設計変更メモ_v2.1.md` U-14）のまま当日を迎える。当日AccessionNumberの下3桁が暫定値であることを装置側の担当者にも伝えておく |
| **Hologic（水端様）** | AE Title / ポート / Days Back・Forward / Max Hits / Modality / 文字コード / MPPS 等 | Conformance Statement記載の既定値（Days Back 60・Forward 2・Max Hits 60・Query Timeout 20分）と食い違う設定になっていないか、現地で最初に画面を見て確認 |
| **Hologic（水端様）追加2点** | ①Patient Heightのマッピングを`(0010,1020)`に明示設定してもらう ②Procedure Code欄は既定で`(0040,0009)`参照（現行プロファイルは送っていない。空欄で問題ないか確認） | 未確認なら§2 S4の項目10・11で当日目視確認 |
| **Calling AE Title** | APEXが名乗ってくるAE Titleを確認し、`config/profiles/BMD_HOLOGIC.jsonc`の`network.allowedCallingAeTitles`に設定 | 未確認なら`callingAeMatching: "ignore"`のまま当日を迎える（誰からでも受け付ける。動作はするが厳密な照合はできない） |
| **病院情報システム** | ネットワーク・FW、**パケットキャプチャ承認**、PACSのUTF-8/Enhanced SR対応 | ⚠ 承認が無いと問題発生時に証跡を持ち帰れない。ダミーデータ案で再提示可 |

### 1-2. ネットワーク構成

- **モダリティ側LANとインターネット側LANを分離**（同一PCの2NIC構成）。APEX自身にセキュリティ機構は無い（Conformance Statement p.69「Apex Modality does not support any specific security measures」）ため、分離自体は妥当。
- ⚠ **2NIC間のIPフォワーディングが無効になっていることを前日に確認。** 意図せずブリッジされていると分離の意味がなくなる。
- `mwm-scp`が待ち受けるのはモダリティ側NICのIPだけでよいか確認（現状は全インターフェース待受）。

### 1-3. PC構成

- **mwm-scpとBRIDGE-Naviは同一PC**（岩田が用意する機体）で動かす。連携点はローカルディスク上のSQLite1ファイルであり、ネットワーク越しの連携は不可（規則5）。
- **BRIDGE-Naviの`<TargetFramework>`を事前確認。** `Ascube.Mwm.Abstractions`/`Ascube.Mwm.Store`は`net10.0`。BRIDGE-Naviが古いframeworkのままだと`ProjectReference`が通らない。
- 当日はインターネット接続・Claude Code・大型ディスプレイが使える想定（9/16時点の岩田の準備計画）。ネットワーク承認の状況次第で変わりうるので、当日朝に再確認する。

### 1-4. 持ち物

- DCMTK（`echoscu`/`findscu`/`dcmdump`）— 自作`mwm-scu`と実装を共有しないため、規格適合の独立検証に必須（検証の注意・実装指示書）
- dumpcap（パケットキャプチャ承認が取れた場合のみ。同梱しない別インストール）
- ノートPC（mwm-scp + BRIDGE-Navi稼働用）、大型ディスプレイ

---

## 2. 当日の段取り

**必ずS0から順に。** 途中で失敗したらその段で止め、原因を切り分けてから先に進む。

### S0：mwm-scpが単体で動く

```powershell
cd C:\dev\ascube-mwm
.\scripts\02_validate.ps1                              # 設定検証（規則8：ミスがあれば新設定を適用しない）
dotnet run --project src\Ascube.Mwm.Scp -- --console --profile BMD_HOLOGIC
```

別ウィンドウで:
```powershell
.\scripts\00_watch.ps1                                  # 常時監視。今日いちばん見る画面
.\scripts\03_echo.ps1                                   # C-ECHO疎通確認（開発機からの自己確認）
```

### S1：APEX側の設定

現地でAPEXの設定画面を開き、以下を確認・入力する（Conformance Statement Table 4.4-2既定値と照合）:

| 項目 | 入力値 | 備考 |
|---|---|---|
| Worklist SCP AE Title | `ASCUBE_MWM` | こちらのCalled AE。1つしか設定できない（Conformance Statement §4.4.1.2.4） |
| Worklist SCP IP/Host | mwm-scp稼働機のモダリティ側NIC IP | |
| Worklist SCP Port | `11112` | |
| Days Back / Forward | 既定60/2のまま推奨 | 変更するとテストの前提が変わるので、変更する場合は04_find.ps1側の`--emulate-apex-defaults`も合わせる |
| Maximum Hits Per Query | 既定60のまま推奨 | 1人モデルなので実害は無いが、既定値からの変更は他の設定ミスの兆候として見ておく |
| Scheduled Station AE Title（自動クエリ用） | 空 or 未設定 | 既定Noneのまま。設定していると絞り込みキーとして送られてくる点に注意 |

### S2：BRIDGE-Navi側の結線確認

BRIDGE-Naviから実際に受診者を1人受け付けさせ、`SetCurrentAsync`が呼ばれることを確認する。

```powershell
.\scripts\01_health.ps1
```
→「現在の受診者：あり（TTL残 mm:ss）」になっていること。**ならない場合はDBパスが両者でずれている**（`docs/連携テスト手順.md` S1の注意と同じ）。

⚠ **BRIDGE-Naviが不調・未結線の場合はここで詰まる。** その場合は §4 のフォールバックへ。

### S3：実機からのクエリ（本丸）

APEX画面で「Query」（または「Detailed Query」）ボタンを押す。

**期待**：APEXのワークリスト画面に受診者が**1件**表示される。

`00_watch.ps1`の画面と`admin explain-query`で、実際に何が起きたかを裏から確認できる:
```powershell
.\scripts\05_explain.ps1 -RunId <直近のRunId>
```

### S4：応答の中身の確認（実機画面で）

**ここが本番で効く部分です。** 1つずつ声に出して確認する（`dcmdump`/`scu preview`で開発機でも見られるものと、APEX実機画面でしか見られないものを両方含む）。

| # | 確認箇所 | 期待 |
|---|---|---|
| **1** | `(0010,0020)` PatientID | **依頼電文の項番3「個人番号」12桁**。★**項番4（受診番号5桁）になっていないか。**間違えると同じ人が毎年別人になり、骨密度の経年比較が壊れる。**1年後まで発覚しません**（規則18） |
| 2 | `(0010,0010)` PatientName | カナが出ている。⚠ BRIDGE-Naviからは**半角のまま**渡すこと。全角化はSCP側（PnEncoder）の仕事 |
| 3 | `(0008,0005)` SpecificCharacterSet | `ISO_IR 192`（案①）。⚠ **コンソール表示で判定しない**（CP932で化ける）。`dcmdump`か16進で見る |
| 4 | `(0010,0030)` / `(0010,0040)` | 生年月日 YYYYMMDD ／ 性別。**`1`でも`2`でもない場合は空値**（`O`にしない） |
| 5 | `(0008,0050)` AccessionNumber | 規則どおり`YYMMDD+受診番号5+受診区分2+検査識別3`で**16桁以内**。⚠ **検査識別3桁が設定値から来ているか**（コードに直書きされていないか）。ヘルゼアの回答待ちの暫定値 |
| 6 | `(0040,0100)` SQ内`(0040,0002)` | ScheduledProcedureStepStartDateが**今日** |
| 7 | `(0020,000D)` StudyInstanceUID | `2.25.`形式。**同じ人を2回`SetCurrent`しても変わらない**こと |
| **8** | `(0010,1020)` / `(0010,1030)` | 当日未測定なら**タグごと存在しない**。★**前回・前々回の値を今日の値として送らない**（規則17）。⚠ **身長はAPEX画面でcm表示になっているか確認**（ascube-mwm内部ではDICOM上メートルに変換して送信しているため。`設計変更メモ_v2.1.md §I`） |
| 9 | 外字テスト | **髙・﨑・德**を含む名前を1件流す。案①（UTF-8）なら通る |
| **10** | APEX画面の「Patient Height」欄（実機限定） | 事前にHologic側でマッピング設定していれば表示。していなければ空欄が正常 |
| **11** | APEX画面の「Procedure Code」欄（実機限定） | 現行プロファイルは`(0040,0009)`を送っていないため空欄の想定 |

### S5：患者取り違え防止（★絶対に落とせない）

1. 受診者A受付 → APEXでクエリ → **Aが表示される**
2. Aの検査完了・退室操作 → BRIDGE-NaviのClearCurrentAsyncが呼ばれる分岐を**実際の画面遷移で**確認（正常終了・中断・スキップ・強制終了の**全分岐**）
3. 受診者B受付 → APEXでクエリ → **Bだけが表示され、Aが混ざらない**

⚠ これは`PatientContinuityTests.cs`で自動テスト済み（W-01/W-02）だが、**BRIDGE-Navi側の実際の画面遷移から呼ばれるかは現地でしか確認できない。** 中断したまま次の受診者が入ってくるのが現場最悪の事故パターン。

### S6：文字コード（必要なら）

初期案①（`ISO_IR 192`）でAPEX画面の氏名表示が崩れる場合のみ:

```powershell
.\scripts\switch-charset.ps1 -Plan 4    # 案④：ISO_IR 13単独・半角カナのみ
```

SCPは止まらない。次のクエリから新設定で応答する。**外字（髙・﨑・德）を含む名前で1件試す**（案①でなければ通らない前提）。

---

## 3. 0件・想定外の結果への対処

**当てずっぽうで直さない。**

```powershell
.\scripts\05_explain.ps1 -RunId <id>
```

- `CurrentEntry`が無い → BRIDGE-Navi側の受付処理が`SetCurrentAsync`まで到達していない
- TTL切れ → 受付から装置クエリまで15分以上経過した（既定TTL）
- ScheduledDateが範囲外 → APEX側のDays Back/Forward設定とBRIDGE-Naviの受診日がずれている
- PatientID/PatientName不一致 → APEXが指名検索（Detailed Query）をかけている。要求値と保持値の両方が表示される
- DatasetBuilder Suppressed → 必須項目（氏名等）が欠損。監査ログに理由が残る

---

## 4. BRIDGE-Naviが不調・未結線のときのフォールバック

S2で詰まった場合、**BRIDGE-Navi抜きで「SCP-装置間は正常」を証明する。**

```powershell
.\scripts\07_set-current.ps1 -PatientId <個人番号12桁> -FamilyKanji <姓> -GivenKanji <名> `
    -FamilyKana <姓カナ半角> -GivenKana <名カナ半角> -BirthDate <YYYYMMDD> -Sex M|F `
    -HeightCm <身長cm> -WeightKg <体重kg> -ProcedureDesc "骨密度(駅前)"
```

APEX側でクエリして表示されれば、**問題はBRIDGE-Navi側にある**と切り分けられる。表示されなければSCP・ネットワーク・装置設定側を疑う。

片付け:
```powershell
.\scripts\08_clear-current.ps1
```

⚠ **これは本番の受診者データ投入経路ではない。** 切り分け専用。

---

## 5. 証跡の収集

```powershell
.\scripts\collect-evidence.ps1     # プロファイル・直近監査ログ・T9生キャプチャ・pcapを1フォルダに集約
.\scripts\mark.bat 3                # 手順の区切りごとに目印を残す（例：3番目のステップ）
```

パケットキャプチャの承認が取れている場合のみ:
```powershell
.\scripts\start-capture.bat
```

問題が起きた場合は、**その場で直そうとせず、まず現象と`RunId`を記録**してから対応を決める（`docs/連携テスト手順.md §9`と同じ方針）。生の通信はraw recorder（T9）が`captures/`に全部残しているので、後から`scu replay --from-audit <id>`で再送・再現できる。

---

## 6. 当日は行わないこと

- `install-service.ps1`によるWindowsサービス化（1日限りのテストのため不要。本番運用開始時に別途実施）
- Calling AE照合の`strict`化を当日いきなり試すこと（`logOnly`で様子を見てから）

---

## 7. 今日の完了条件（サインオフ基準）

以下がすべて埋まれば、今日はここで終了してよい。**全部揃わなくても、どこまで進んだかをはっきりさせて終わる**のが目的（曖昧なまま持ち帰らない）。

- [ ] S0〜S3が通った（実機からのC-FINDで受診者1件が表示された）
- [ ] S4の11項目を確認した（実機限定の10・11を含む。空欄が想定どおりかも判断した）
- [ ] S5（患者取り違え防止）を、BRIDGE-Naviの実際の画面遷移の**全分岐**で確認した
- [ ] 今日中に届かなかった項目がある場合、それが**§1-0のどの項目のせいか**を特定した（「なんとなく動かなかった」で終わらせない）
- [ ] 各段の結果を`docs/progress.md`に記録した（次節）

⚠ **S0〜S3が通らないまま時間切れになった場合**、§4のフォールバック（`admin set-current`）で「SCP-装置間だけは正常」の切り分けだけでも済ませてから終了する。BRIDGE-Navi側の問題なのか装置設定の問題なのかを持ち帰れる状態にしておく。

---

## 8. 記録

各段の合否を`docs/progress.md`に1行追記（日付・段ID・合否・残課題）。9/28終了後、今日の結果を踏まえて次のステップ（本番リリース前チェックリスト・第2弾横展開）を判断する。
